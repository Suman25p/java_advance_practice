server:
 port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Suman25p/microservice-config.git
          default-label: main
          clone-on-start: true 
          